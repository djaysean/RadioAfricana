diff --git a/node_modules/react-native/React/CoreModules/RCTExceptionsManager.mm b/node_modules/react-native/React/CoreModules/RCTExceptionsManager.mm
index cdaae3a..1d6ce8c 100644
--- a/node_modules/react-native/React/CoreModules/RCTExceptionsManager.mm
+++ b/node_modules/react-native/React/CoreModules/RCTExceptionsManager.mm
@@ -157,6 +157,23 @@ RCT_EXPORT_METHOD(reportException : (JS::NativeExceptionsManager::ExceptionData
   double exceptionId = [errorData[@"id"] doubleValue];
 
   if ([errorData[@"isFatal"] boolValue]) {
+    RCTLogError(
+        @"[RadioAfricana Diagnostic] FATAL JS EXCEPTION\n"
+         "message: %@\n"
+         "originalMessage: %@\n"
+         "name: %@\n"
+         "componentStack: %@\n"
+         "stack: %@\n"
+         "exceptionId: %@\n"
+         "extraData: %@",
+        errorData[@"message"],
+        errorData[@"originalMessage"],
+        errorData[@"name"],
+        errorData[@"componentStack"],
+        errorData[@"stack"],
+        errorData[@"id"],
+        errorData[@"extraData"]);
+
     [self reportFatal:message stack:stack exceptionId:exceptionId extraDataAsJSON:extraDataAsJSON];
   } else {
     [self reportSoft:message stack:stack exceptionId:exceptionId extraDataAsJSON:extraDataAsJSON];
diff --git a/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm b/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
index 516ae35..3a5b99a 100644
--- a/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
+++ b/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
@@ -392,9 +392,21 @@ id ObjCTurboModule::performMethodInvocation(
       TurboModulePerfLogger::asyncMethodCallExecutionStart(moduleName, methodName, asyncCallCounter);
     }
 
+    RCTLogError(
+        @"[RadioAfricana Diagnostic] TurboModule invocation: %s.%s (sync=%d)",
+        moduleName,
+        methodNameStr.c_str(),
+        isSync ? 1 : 0);
+
     @try {
       [inv invokeWithTarget:strongModule];
     } @catch (NSException *exception) {
+      RCTLogError(
+          @"[RadioAfricana Diagnostic] TurboModule exception: %s.%s — %@: %@",
+          moduleName,
+          methodNameStr.c_str(),
+          exception.name,
+          exception.reason);
       if (isSync) {
         // We can only convert NSException to JSError in sync method calls.
         // See https://github.com/reactwg/react-native-new-architecture/discussions/276#discussioncomment-12567155
@@ -457,12 +469,25 @@ void ObjCTurboModule::performVoidMethodInvocation(
 
     TurboModulePerfLogger::asyncMethodCallExecutionStart(moduleName, methodName, asyncCallCounter);
 
+    RCTLogError(
+        @"[RadioAfricana Diagnostic] TurboModule invocation: %s.%s (void)",
+        moduleName,
+        methodNameStr.c_str());
+
     @try {
       [inv invokeWithTarget:strongModule];
     } @catch (NSException *exception) {
-      // Void methods are always async, re-throw instead of converting to
-      // JSError, same as the async branch in performMethodInvocation.
-      @throw exception;
+      RCTLogError(
+          @"[RadioAfricana Diagnostic] TurboModule exception: %s.%s — %@: %@",
+          moduleName,
+          methodNameStr.c_str(),
+          exception.name,
+          exception.reason);
+      RCTLogError(
+          @"[TurboModule] Exception in async void method %s.%s: %@",
+          moduleName,
+          methodNameStr.c_str(),
+          exception);
     } @finally {
       [retainedObjectsForInvocation removeAllObjects];
     }
